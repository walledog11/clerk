
services:
  redis:
    image: redis:alpine
    container_name: clerk-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data # This saves your queue data even if Docker restarts
    restart: unless-stopped

volumes:
  redis_data: